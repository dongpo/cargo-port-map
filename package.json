{
  "name": "cargo-port-connectivity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "build:pages": "vite build --mode pages"
  },
  "dependencies": {
    "maplibre-gl": "^6.11.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "topojson-client": "^3.1.0",
    "vite": "^7.0.0",
    "world-atlas": "^2.0.2"
  }
}
